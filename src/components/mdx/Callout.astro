---
/**
 * Callout — highlighted text block for important notes in case studies.
 *
 * Renders a styled box with an accent left border. Use it in MDX to
 * call attention to key insights, design decisions, or takeaways.
 *
 * Usage in .mdx:
 *   import Callout from "../../components/mdx/Callout.astro";
 *
 *   <Callout>
 *     This is an important insight about the design process.
 *   </Callout>
 */

interface Props {
  /** Optional label shown above the callout text (e.g., "Key Insight"). */
  label?: string;
}

const { label } = Astro.props;
---

<aside
  class="my-8 rounded-r-lg border-l-4 p-6"
  style="border-color: var(--theme-accent); background-color: var(--theme-bg);"
>
  {label && (
    <p class="mb-2 text-xs font-bold uppercase tracking-wider" style="color: var(--theme-accent);">{label}</p>
  )}
  <div class="leading-relaxed" style="color: var(--theme-text-secondary);">
    <slot />
  </div>
</aside>
